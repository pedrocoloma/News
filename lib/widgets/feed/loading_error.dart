import 'package:flutter/material.dart';

class LoadingError extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('An error occured while loading.'),
    );
  }
}
